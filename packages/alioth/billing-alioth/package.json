{
  "name": "@dsh-alioth/billing-alioth",
  "description": "Billing capability: ctx.aliothBilling service — subscription lifecycle, monthly bills, invoice requests (volatile memory provider; the real backend swaps in behind the same interface)",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/schemastery": "^3.18.1"
  }
}
