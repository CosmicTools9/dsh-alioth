// Handler integration tests require a running PostgreSQL instance
// These tests verify the encoding HTTP API surface

#[cfg(test)]
mod handler_tests {
    // Integration tests are run with:
    // cargo test --test handler_integration
}
