# SSO Backend — 测试模式环境变量（test mode）
#
# 复制为 .env.test 可覆盖测试运行时的数据库连接。该文件不纳入版本控制（.gitignore）。
#
# 默认情况下 `mise run test` / `mise dev:test` 会自动使用规范测试库连接串：
#   postgres://$(whoami)@localhost:5432/aliothstudio_test
# （与 scripts/db/reset-db.sh 的 whoami 约定一致，当前 OS 用户即 aliothstudio_test 属主）
#
# 仅在需要自定义时创建 .env.test，例如 CI 环境使用带密码的 URL：
#   DATABASE_URL=postgres://sso_test:secret@db-host:5432/aliothstudio_test
#
# 也可以通过环境变量（不经过文件）覆盖：
#   SSO_TEST_DATABASE_URL=postgres://user:pass@host:5432/aliothstudio_test mise run test

# 测试数据库连接（留空则使用上述默认规范测试库）
# DATABASE_URL=postgres://$(whoami)@localhost:5432/aliothstudio_test
