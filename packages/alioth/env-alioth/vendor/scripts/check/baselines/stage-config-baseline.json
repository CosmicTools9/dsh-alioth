{
  "schema": 1,
  "note": "stage_config.yaml 死配置基线（SPEC_ENFORCEMENT_SUBSTRATE §基线切分）——dead_fields 为已知欠债（当前无人消费的配置字段：零引用 + 仅解析面引用），--fail 仅阻断基线之外新增的未消费字段；修复后从 dead_fields 移除对应路径并提交。",
  "updated_at": "2026-09-25",
  "dead_fields": [
    "repair_limit",
    "stages[].has_human_gate",
    "stages[].human_gate_prompt"
  ]
}
