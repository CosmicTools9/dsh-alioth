strict-peer-dependencies=false
engine-strict=false
# Pin to the official registry: npmmirror lags on @embedded-postgres beta
# packages (18.4.0-beta.17 404s there). Local mirrors must not break installs.
registry=https://registry.npmjs.org/
