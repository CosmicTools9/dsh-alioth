# Repository Guidelines

## 核心决策原则

**准确·稳定·一致性优先**——一切工具选型、方案设计、代码变更以此为标准：准确先于快速，稳定先于新奇，一致先于便利。

- 结构化格式（CSS/HTML/JS/JSON/YAML/TOML/SQL）必须用专用解析器，禁止正则模拟（见 `docs/specs/NO_REGEX_FOR_PARSING.md` 同源规范）；本仓库的离线生成/门禁脚本对固定行格式的种子/DDL 做行级提取属例外，但新解析一律先查现成实现。
- 工具链固化：同一类操作只留一个入口（pnpm scripts / mise tasks / lefthook），不重复造门禁。
- 现成方法优先：实现/修复/重构前必须四查现成实现（AGENTS.md 规约 / scripts 工具 / vendored 产物 / 上游 harness 包），有则不重写。
- 插件对齐 **AppAgent**（app-agent 状态机 + skill-adapters + 产物契约）——不迁移 AliothStudio 的 Meta/Gateway 开发约定（cargo/Rust 门禁、DB schema truth via Meta、SSO/NGAC、前端框架禁令等一概不适用）。

## 核心边界表

| 类型 | 规则 |
|---|---|
| ✅ Always | 程序化生成优先：产物一律走 gen-alioth 生成器 + 契约门工具（`alioth_app_write`/`alioth_app_configure`/`alioth_entity_write`），LLM 只供结构化参数与语义决策，禁止直写产物文件；模型面改动必须更新 keyless 快照（`tests/model-surface.spec.ts`）；namespace 参数一律先 `alioth_workspace_current` 解析，禁止猜测；提交前过 lefthook + CI 门禁；改动 AGENTS.md 数字（测试数/字典规模）同 PR 刷新 |
| ⚠️ Ask | 破坏性操作（删账号/删产物/改门禁/改 CI）；模型发行物同步（vendor 重生成 + `resetRegistry` 会清本地注册实体）；新增外部依赖；迁移 AppAgent 管线契约 |
|🚫 Never|用 `read` 工具读 vendor/模型快照文件（用 `alioth_workflow_info` 内省）；直接 SQL 写 `isahl_meta`/`dsh_alioth_auth`（产品工具是唯一写入通道，人工操作除外）；**任何情况下写 schema `isahl`**（那是 **NS:Alioth 的业务表空间**——`AliothStudio/Deploy/Alioth`（Gateway+SSO）那套部署的库，dev 上一度与 dsh-alioth 同库共处，2026-09-24 契约修正后已分离：dsh-alioth 的注册表只在它自己的 `dsh_alioth` 库；dsh-alioth 只读 `isahl`；**该铁则由 `tests/iron-rule-no-isahl-sql.spec.ts` 机械守住**——src/scripts 里出现 `isahl.<relation>` 形态的 SQL 即红）；静默丢弃未提交改动（删除前先 stash 兜底）；agent 代执行 DB 域批量操作（重置/迁移/种子刷新——交付 SQL 或命令，执行归用户）|
| 🔍 Audit | 动 AppAgent 管线/契约/架构/安全前，对照本文件与 skill-alioth 契约（`agent-contract.ts` serde-alias 兼容）逐条核对；诊断与处方分离——只读审查任务给结论，不附代执行提议 |

## 规约刷新分层

| 层级 | 内容 | 刷新时机 |
|---|---|---|
| L-会话级 | 本 AGENTS.md 全文 | 会话启动、上下文压缩恢复后 |
| L-任务级 | 命中的技能 SKILL.md、契约文件 | 每步决策前（换任务重读） |
| L-冷层 | 未命中内容 | 永不重读 |

任务特征变化使冷层行命中时，该行升级为任务级——分层是刷新节奏约束，不是信息屏蔽。新增任何协议/文档前先分层，控制层自身不得成为过载源。

## 已知缺口（模型通道现状）

- **模型发布物（`github:CosmicTools9/Alioth`，如 v10.0.33）只含物理 DDL（`002_isahl_tables.sql`，0 条 REFERENCES 约束）+ 维度种子——不含 `isahl_meta` 注册表**。注册表语义（名称/类目/继承/`reference_config`）分两条通道来，**都不纳入 GIT**（派生数据，随模型演进重生成）：① AliothStudio 发布管线在模型仓库根生成 `isahl_meta-registry.sql`（`pg_dump --data-only --inserts --on-conflict-do-nothing isahl_meta.meta_collections/meta_fields`（位置式 VALUES，不带列名），**落点 = 模型仓库根，唯一消费坐标**；2026-09-24 起 AliothStudio 侧不再生成 `Framework/seed/` 中间副本——该副本无消费方且会污染种子载体判据面，见上游 change `fix-meta-registry-artifact-face`；不入 `MODEL_ARTIFACT_FILES`/`latest.json`、不入 GIT；命名刻意不用保留前缀 `seed-*`——那是 seed 重放载体的命名空间，`Framework/seed/seed-declaration-files.json` 单一来源门禁会要求入册；发布前断言 `schema <> 'isahl'` 行数为 0，非模型行即 fail-loud）；② 包内**不带**注册表行：`builtin` 于 2026-09-24 退役，`modelSource` 必需（`env-alioth` 对 `'builtin'` 直接报错，不给静默回落）。部署侧用 `mise run alioth:model-source -- --source <模型发行物|本地模型树> --out <dir>` 组装一份源（发行物内容 + 侧车行 + 包内套件；`--fixture-seed` 供无网络自检），组装器缺行或缺套件即非零退出；`fk-index.json` 由该源的 sidecar 派生（2026-09-24：2661 → **3528** refs，随 905→986 collections 而变），源里没有 sidecar 时字典门禁退化为「仅锚定字节」。**消费契约（2026-09-24）**：注册表行**只**来自模型快照——**官方发行必然带它**（发布管线生成的专用拷贝 `isahl_meta-registry.sql`；部署侧由 `mise run alioth:model-source` 把它并进组装源——包内不再冻结任何副本），注册表信息进 harness agent 上下文；**开源/独立副本不带时自动退化**（`registrySource: 'missing'`）：boot **只告警不阻塞**，`isahl_meta` 起为空注册表，agent 只依赖 Alioth 自身 DDL 携带的元数据（离线字典快照即该 DDL 的抽取物：`physical-tables.json` 表与继承、`coordinates.json` 坐标码；`fk-index.json` 因发布物 0 条 REFERENCES 约束无法从 DDL 得到，随之不可用）。绝不"借用"快照未声明的行（早期版本曾回落包内冻结副本，2026-09-24 撤销：那会把 A 发布物的行注进 B 环境）。**sidecar 的两个面归消费侧加载器处置**（发布侧只剥离会话级 `search_path` 改写——上游 change `fix-model-sidecar-lifecycle`，其余前导不剥离）：psql 信封 `\restrict`/`\unrestrict` 与 dump 会话前导（`SET …`；`set_config('search_path','',false)` 官方发布侧已剥离、包内冻结副本仍含）都丢弃——本插件持**连接池长连接**，前导会越过加载期（实测未剥离时：加载后整个会话 `search_path` 变空、`client_min_messages`/`row_security`/`lock_timeout` 停在 dump 取值）；dump 中除「`isahl_meta` 两表 INSERT」外的语句一律 fail-loud 拒绝（`registry-ddl.ts`）。结构基线（`002_isahl_meta_schema.sql`）恒用包内冻结版本，故"只带行不带结构"的快照也能引导。发布物布局两态都认（平铺固定路径 + 旧的 `<repo>/<version>/`），`latest.json.version` 作为 `modelVersion`（模型仓库不带 `alioth-gen/src/lib.rs`）。
- 语义库（skill-alioth/src/data/）为混合来源：`coordinates.json`/`physical-tables.json` 可从发布物离线提取（`check:dicts` 真门禁）；`fk-index.json` 从**模型源的 sidecar** 提取（不能从 isahl DDL 来——发布物 0 条 REFERENCES 约束 ⇒ 外键目标不在模型自身内容里）；源里没有 sidecar 时（CI 克隆即此形态）门禁退化为「仅锚定字节」，该面只在本机带模型源时真校验。
- 模型演化后的本地注册表需 `mise run alioth:doctor --reset` 重引导（清本地自定义实体）+ 语义索引按 entriesHash 自动重建。

## Project Overview

`dsh-alioth` is a [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness) plugin that leverages the **Alioth V10 data model** to deliver **AppCreator** capabilities: a dialogue-driven enterprise app generator. The agent (running on the harness) turns a user request into AliothStudio-compatible artifacts — `app.json`, `module.json`/`block.json`, `extensions/*.yaml`, HTML prototypes, and backend `Sources/` skeletons — through the Alioth model pipeline (AppAgent state machine + `alioth-gen` + `ontology-mapping`), backed by a PostgreSQL `isahl_meta` schema.

The plugin group is authored against two sibling checkouts used as reference:

- `/Users/william.d.zk/WorkSpace/deepseek-harness` — the host harness (plugin-based agent framework on vendored Cordis; "everything is a plugin", upstream `deepseek-ai/deepseek-harness`).
- `/Users/william.d.zk/WorkSpace/AliothStudio` — the Alioth platform (model v10.0.0). `AppCreator/` inside it is the standalone consumer the plugin reproduces as harness-native capability.

Positioning (from `AppCreator/DESIGN_INTENT.md`): AppCreator is the open-source standalone consumer entry to the Alioth model — needs only PostgreSQL + an LLM API key, shares the AppAgent pipeline via vendored crates (`vendor/app-agent`, `vendor/alioth-gen`, `vendor/ontology-mapping`, `vendor/runtime-engine`), and its output imports cleanly into AliothStudio.

## Architecture & Data Flow

### Harness side (host)

- **Plugin model (Cordis)**: a plugin exports `name`, `inject`, `apply(ctx, config)`, and a schemastery `Config`; it contributes services, typed events, and reversible effects to a shared `Context`. All registrations go through `ctx.effect()` / `ctx.on()` / `ctx.waterfall()`; a registry's `register()` returns a disposer. Every part of the product — model adapter, tool registry, session log, agent loop — is a replaceable plugin.
- **Agent loop**: one *step* = one model request plus its tool calls; a *turn* = zero or more steps. Durable session events (`turn/*`, `step/*`, `user/message`, `assistant/*`, `tool/*`) persist to an append-only `SessionEvent` log; live extension events (`agent/*`, `tools/*`, capability events) observe/intercept work in flight. `agent/pre-step`, `agent/request`, `llm/stream`, and the three `tools/*` events are waterfalls — listeners MUST call `next()`.
- **Capability seam** = Service Definition + Service Provider + Consumer (commonly a model-facing tool). Adding a capability means designing all three roles. A data-backed tool is registered with `defineTool(...)` on `ctx.tools`; its JSON schema joins prompt assembly automatically.
- **Profiles & bundles**: a running `dsh` is a plugin tree composed from ordered layers — bundles (patch layers, `dsh.bundle` in package.json) stacked by profiles (`dsh.profile`, templates `web`/`headless`). Inspect your boot tree with `dsh --profile web --dump-config`.
- **Model-visible ⟺ logged**: anything that reaches a model request must be reconstructable from the session log; new model-visible input requires a new `SessionEventMap` member.

### Alioth side (data model the plugin operates on)

- **Alioth 模型 v10.0.0** — single source of truth: `AliothStudio/Meta/backend/alioth-gen/src/lib.rs` (`ALIOTH_MODEL_VERSION`, env `MODEL_VERSION`, default `10.0.0`).
- **Ontology → DB mapping**: `docs/specs/ALIOTH_ONTOLOGY_SPEC.md` is the unique mapping truth. Business entities map onto lifecycle leaf tables in the `isahl` schema (`zc_id_lifecycle` inheritance family); `isahl` forbids `CREATE TABLE`/`ALTER TABLE` — new entity types extend via `meta_collections` / `meta_fields`. **Schema truth is the DB**: query live with `mise run schema-info`, never read DDL files.
- **Artifact model**: `app.json` (requires `namespace`, plus `config.modules/blocks`, `permissions`, `routing`, `navigation`, `min_alioth_version`), `module.json`, `block.json`; authoritative paths under `Pre-Proc/{namespace}/`. Concrete example: `AliothStudio/Pre-Proc/Alioth/Apps/ai-i-need-a/app.json`.
- **AppAgent pipeline**: `Meta/backend/app-agent/` — a state machine consuming `skill-adapters/*.yaml` (typed Track/Step/gate definitions: `alioth-app.yaml`, `alioth-module.yaml`, `alioth-block.yaml`, `alioth-compose.yaml`, `alioth-ontology.yaml`, ...). This is the pipeline the plugin drives to create apps.
- **Data flow**: dialogue request → AppAgent state machine (tracks/steps/gates) → `alioth-gen`/`ontology-mapping` engines → Pre-Proc artifacts (`Pre-Proc/{namespace}/Apps/{app}/app.json` + `extensions/` + `prototype.html` + `gateway_design.md` + `Sources/` skeleton) → importable by AliothStudio/Gateway. Plugin bridge shape: harness tool → Rust binary/API (app-agent / alioth-gen CLI) → PostgreSQL `isahl_meta` → artifacts.

## Key Directories

### Harness (`/Users/william.d.zk/WorkSpace/deepseek-harness`)

- `packages/<group>/<pkg>/` — `@deepseek-ai/dsh-*` workspaces; `core/` (session, system-prompt, tools, agent, agent-loop), `llm/`, `session/`, `skill/`, `shell/`, `fs/`, `web/`, `subagent/`, `todo/`, `bundle/`, `examples/`, `util/`. Full map: `packages/README.md`.
- `apps/cli`, `apps/web` — bins. `docs/` — architecture, testing, cookbook. `examples/` — runnable `cordis.yml` leaves + e2e/snapshot scenarios. `scripts/` — repo gates (`run-gates.ts`) and generators. `.agents/` — workflows, skills, Agent Notes.

### Alioth (`/Users/william.d.zk/WorkSpace/AliothStudio`)

- `Meta/` — metadata platform (backend Rust :4949, frontend React :49494); `app-agent/` = AppAgent pipeline.
- `AppCreator/` — independent deliverable: own Cargo/pnpm workspace, `backend/` (Rust, `vendor/` crates), `Pre-Proc/Alioth`, `skill-adapters/` (vendored copy, synced by `scripts/sync-framework.sh` — never hand-edit), `scripts/` (`dev.sh`, `prototype-tool.js`, `db/`, `check/`).
- `Gateway/` (business runtime, SSO auth), `SSO/`, `Framework/` (shared infra), `Pre-Proc/{namespace}/` (generated `Apps/`, `Sources/`, `Prototypes/`, `_schema`, `seed`, `openapi/`), `skill-adapters/` (AppAgent YAML), `docs/specs/` (executable specs), `scripts/`, `Samples/`.

### This repo (`dsh-alioth`)

- `packages/alioth/tool-alioth/` — `alioth_app_list` (discovery: enumerate namespaces/apps with contract validity), `alioth_app_inspect` (read/validate `app.json`；读回含模型依赖：`modelVersion` = 本部署的模型、`modelVersionSatisfied` = 声明的 `min_alioth_version` 是否被满足，任一侧不可判定即 false), `alioth_app_write` (generate + persist contract-validated app tree: app.json, module.json, extensions skeletons, Sources dirs; refuses overwrite; `approvalMode` `required`|`bypass`, default bypass), `alioth_app_configure` (enrichment + growth: merge brand/navigation/routing/permissions/status/goal/non_scope AND add modules — each new module gets a contract-valid module.json + Sources dir and joins config.modules/navigation — or replace blocks), `alioth_app_delete` (destructive, irreversible: requires `confirm: true`, approval seam shared with write; archive via configure `status`). `examples/alioth-agent/` composes the Alioth overlay for headless + web profiles. **模型依赖落进产物**：写入面把本部署的模型版本打进版本锚（`app.json.min_alioth_version`、`service.json.aliothVersion`；env 不可达退契约下限 `10.0.0`，非发行形态的模型源——夹具/git ref——绝不写成依赖），单一比较来源是 `gen-alioth/src/version.ts`。
- `packages/alioth/tool-alioth-meta/` — `alioth_schema_info`: read-only registry queries (entities/entity/search-fields) over `ctx.aliothEnv.sql()`；dev-seed test entities (`-testing`/`-test` suffixes) hidden by default, `filteredTesting` count reported, `includeTesting` opt-in。**DDL-only 退化（2026-09-24）**：`registrySource === 'missing'`（开源副本不带注册表行）时不再返回空集——`entities`/`entity` 改由包内 DDL 抽取物供表级清单（`physicalTableParents`：表 + 继承父 + 由父链推的 depth），`search-fields` 明确报不可用，输出带 `degraded: true` + `note`（render 前缀 `[DDL-only]`），让模型看得见"缺注册表"而不是把空集当"没有这张表"；`loadSemanticEntries` 同判，退化时索引 DDL 表名（986 条）代替注册表词条，语义检索仍能落在真实表上。`alioth_schema_semantic_search`: multilingual semantic grounding (synonyms + cross-language, e.g. "inventory balance" hits 状态-库存) via transformers.js + Xenova/bge-small-zh-v1.5 (512 dims); model downloads on first use (endpoint `https://hf-mirror.com`, override `DSH_HF_ENDPOINT`), vectors cached under `<dataRoot>/semantic/` keyed by entries hash; real-model probe: `pnpm exec tsx packages/alioth/tool-alioth-meta/tests/probe-semantic.ts` (needs local dev DB). `alioth_entity_write`: register a new business entity on an existing isahl physical table (isahl forbids CREATE TABLE) — validates via skill-alioth `entity-validate` (naming/physical-table/conflicts/inheritance/references/real coordinate dictionary), then INSERTs meta_collections + meta_fields (config: depth/source dsh-alioth/inherits/category/coordinates; reference_config per the FK-index shape); `approvalMode` required|bypass.
- `packages/alioth/gen-alioth/` — artifact contracts (`src/contracts/*.schema.json`, validated by the dependency-free engine in `validate-engine.ts`; **block 契约的必填集 = check-block-json 的 R2**（id/name/version/services/coordinates/aliothVersion/sharing），`coordinates` 无类型约束以承载骨架期 null) + pure generators (`generateApp`, `generateModule`, `generateBlock`（上游 create_block_scaffold 同形骨架）, `generateExtensions`, `generateService`, `sourceModuleDirs`; `generateApp` 现按上游评估面必需集（`evaluate.rs:234-242`）输出 `status`（默认 `developing`），不再留给下游阶段补写); contract-alignment tests against hand-written fixtures. `src/version.ts`（模型版本依赖的单一比较来源：`parseModelVersion`/`compareModelVersions`/`satisfiesModelVersion`/`modelVersionAnchor`/`displayModelVersion`——生成期打锚、工具面与控制台判依赖都用它；`module.json` 不参与：上游 MODULE_SPEC 无模型版本键，module 的依赖由所属 app 承载）。
- `packages/alioth/skill-alioth/` — adapter orchestration primitives: YAML parsing (`adapter.ts` — 另含 **未知 gate 键即 throw** 的 fail-closed 解析、门内容谓词 `require_json_pointer`/`require_json_equals`（RFC 6901 + mtime 最新命中文件 + 深比较，fail-closed）、步骤相位 `phase: plan|apply` 与两条加载期不变量（plan 步必须有 output_glob、其后同 track 必须有 apply 步）、`validateAdapterStructure`）, track/step state machine (`state.ts`), gate checks (`gates.ts`，output-glob + program hook + 内容谓词; 失败携带 `GateResult.repair` 结构化修复契约), repair contract (`repair.ts` — `RepairClass{fixable,retryable,not-fixable}` + 规则表 `ruleId`/`suggested_action`/`evidence`，错误文本带 `[rule:<id>]` 信封，`ruleIdFromError` 为唯一解析点), retry budget (`retry-budget.ts` — 调用签名 ping-pong → terminate；错误签名修复墙 → 第 2 次 trim-retry、第 N 次 escalate，签名优先取 ruleId 以免输出漂移打败墙), file-backed run state (`workspace.ts`；`loadRun` 缺文件即落一份 fresh run，只读面用 `readRun`）, adapter-tool→harness-tool mapping (`mapping.ts`: 覆盖上游 adapter 实际出现的全部工具词汇——read_file→read、write_file→write、patch_file→edit、search_files→glob/grep、search_content→grep、list_dir→read、inspect_image→read_image、http_get→web_fetch、run_command/run_code/file_diff/eval→bash、start_service/stop_service/service_logs→terminal_open/terminal_close/terminal_read、code_intel→lsp、todo→todo_write、defer_blocker→alioth_deferred；`debug_*`/`lint_file`/`openspec_status`/`visual_verify` 无等价物 → `MANUAL_ADAPTER_TOOLS` 逐条写明人工路径，测试断言词汇表完备性), external program runner + bun probe (`bun.ts` — the distribution's prototype gates run `bun scripts/prototype-tool.js`; not ported, bun is a declared deployment dependency). Entity write-path validation (`entity-validate.ts`): naming, physical-table existence (isahl forbids CREATE TABLE — entities map onto existing physical tables), conflicts, inheritance (exists/acyclic/depth), reference integrity (target/junction ∈ registry∪physical; local_key ∈ FK index ∪ root columns), coordinates — **no degradation**: scene/factor/function checked against real dictionary snapshots in `src/data/` (`coordinates.json` 644 codes — scene 108 / factor 118 / function 418, `fk-index.json` 3528 physical refs, `physical-tables.json` 986 tables + 9 root columns), generated offline from the Alioth repo + the vendored registry sidecar by `scripts/generate-semantic-dicts.ts` (writes the tamper-evidence `anchor.json`; freshness gate `pnpm run check:dicts` regenerates and diffs — same pattern as AliothStudio's `fk_index.rs`; 该脚本同时认两种模型发行物布局：`<repo>/<version>/` 与 2026-09-21 起的**平铺固定路径**（文件在仓库根，版本号在 `latest.json`/annotated tag）).
- `packages/alioth/env-alioth/` — `ctx.aliothEnv` service: model snapshot pull (default `github:CosmicTools9/AppCreator@main`, cached per SHA under `dataRoot`; or local path), PostgreSQL 18 from the deployment (host server on dev/m2/prod, PGDG build in the container; the DSN is required — no embedded fallback), `isahl_meta` bootstrap (schema-first; baseline is load-once, non-idempotent — 注册表表缺失时不再一律拒绝：schema 内没有自有表就在**同一往返事务**里先清掉基线自有的 `CREATE TYPE`/`VIEW` 残留再跑基线（清除一律 RESTRICT，有依赖方即整体回滚），持有他方表才拒绝；存在性探测读 `pg_class`/`pg_namespace` 而非 `information_schema`（后者受角色权限过滤，会把活的注册表看成空的）, provenance stamp in `dsh_alioth.model_state` (drift reported, never auto-migrated), `sql()` query surface（唯一查询漏斗 = `PgHandle.query`：连接空闲期死掉后 pg 以「connection error and is not queryable」拒发——该错误证明语句从未发出，句柄**重连一次并重放**；而可能已执行的连接级失败（`Connection terminated unexpectedly`/`57P01`）**绝不重放**，只替换连接、下句先重连——写口径不会被双执行；bootstrap/doctor 同走 `QueryFn`，无人再持裸 `Client`）, `resetRegistry()` (drops registry schemas, re-bootstraps on next `ready()`; handle is reused, cluster stays up), read-only `doctor()`, `modelInfo()`（只解析模型源——**不连数据库、不引导注册表**，显示面据此报模型版本：控制台面板与 `alioth_app_inspect` 都是打开即读，不得有副作用）. Config `databaseUrl`/`modelSource`/`dataRoot`, env `ALIOTH_DATABASE_URL`/`ALIOTH_MODEL_SOURCE`/`ALIOTH_DATA_ROOT`. `mise run alioth:doctor` (exit 0 = green; `--reset` flag re-bootstraps). Network tests gated by `DSH_ALIOTH_NETWORK_TESTS=1`. Gap: public tree ships no `Pre-Proc/Alioth/_schema/*.schema.json` — contracts are hand-written in gen-alioth. **部署约定（2026-09-15；2026-09-24 收敛）**：dev、m2、prod 都跑**宿主 PostgreSQL 18**，容器跑镜像内的 PGDG 18、CI 跑 `postgres:18` service——**已无 embedded 通道**（依赖与回退代码都已移除，DSN 缺失即 boot 失败，不再静默起一个私有集群）——DSN 单独放 `~/.dsh-alioth.env`（0600）：dev 由 `~/.zshenv` 引入，m2 由 launchd 包装脚本 `~/bin/dsh-alioth-web.sh` 引入（prod 是 systemd `EnvironmentFile=/etc/dsh-alioth/env`）；dev 与 m2 的 embedded 数据目录已删除（迁移前的 dump 留在 `~/.dsh-alioth-backups/`）。模型样例库名：`alioth`（dev、m2、prod 同名——**这不是 dsh-alioth 的库**，dsh-alioth 只读它或不连它）。dev 的 `alioth` 是 **Alioth 模型的样例库**——模型发行物 `~/WorkSpace/Alioth/v10.0.x/` 就是它的 pg_dump 导出（`001_schema.sql` 建 `isahl`、`002_isahl_tables.sql` 为 `isahl` 全表结构、`seed-dimensions.sql` 为维度种子）；NS:Alioth 的 `AliothStudio/Deploy/Alioth`（Gateway+SSO，`.env` → `postgres://isahl@localhost:5432/alioth`）以它为业务库运行；AliothStudio 的开发线用 `aliothstudio_dev` 等，**不用** `alioth`。**契约修正（2026-09-24）：dsh-alioth 的注册表数据库是它自己的库 `dsh_alioth`，绝不再落进模型样例库。** 2026-09-15 的「同机同 PG、同库不同 schema」约定是错的：`isahl_meta` 是**模型也拥有的命名空间**，把它建在模型库里等于把本插件的表、baseline 的 `DROP … RESTRICT` 修复与 `resetRegistry()` 的 CASCADE 都放在模型的对象旁边（实测：dev 的 `alioth` 库里一度有我们建的 `meta_collections`/`meta_fields`，且该库 `isahl_meta` 里还留着模型侧的孤立枚举）。现已由代码 fail-closed 拦死：`bootstrapDatabase()` 每次调用先查 catalog，目标库 `isahl` schema 若有普通表即判为「模型库」并**拒绝引导**（`assertNotModelDatabase`，`env-alioth/src/bootstrap.ts`），创建/领养/drift 报告三条路径都拦；容器入口 `scripts/docker-entry.sh` 自动建 `dsh_alioth` 库（旧卷把 `alioth` 就地 `ALTER DATABASE … RENAME`）。库角色对自己的库需 `CREATE, CONNECT`（否则 `CREATE SCHEMA IF NOT EXISTS` 报 42501）；`dsh_alioth` 库里 `--reset`/`resetRegistry()` 是安全的（只清 `isahl_meta` + `dsh_alioth`，**不动 `dsh_alioth_auth` / `dsh_alioth_billing`**——账号与已付费的 L2 授权必须活过注册表重置）。**铁则不变：任何情况下不写 schema `isahl`**（模型样例的 986 张 `isahl` 物理表 = NS:Alioth 的业务表空间，dsh-alioth 只读，代码里对 `isahl.*` 零条 SQL）。存量环境的搬库/摘除步骤（dev/m2/prod 各自执行）：`docs/migrations/2026-09-24-registry-out-of-model-database.md`。**三处的 `alioth` 库都装配模型样例**（`isahl` schema：967 表 / 24 函数 / 6 序列 + 样例数据，`zc_ad_object` 2984 行）：dev 由 NS:Alioth 的 `Deploy/Alioth/seed/*` 装配，m2/prod 由 `pg_dump --schema=isahl`（dev 导出，`--no-owner --no-privileges`）同步。样例含 10 个 `geometry` 列，**PostGIS 必须装进 `postgis` schema**（`CREATE SCHEMA postgis; CREATE EXTENSION postgis SCHEMA postgis`）——装到 `public` 会让那 10 张 `zc_id_geog-*`/`zc_id_geom-*` 建表失败（`schema "postgis" does not exist`）。
- `packages/alioth/tool-alioth-workflow/` — model-facing AppAgent workflow bridge: `alioth_workflow_step` (current track/step instruction + tools + gates from the snapshot's skill-adapter；载荷含 `phase`/`planWriteGlobs`，门以 `GateView` 完整只读形态透出（含 `require_json_pointer`/`require_json_equals` 谓词）), `alioth_workflow_complete` (runs gates — artifact globs on disk + program gates via the bun runner — advances the deterministic state machine, persists run state under `<dataRoot>/workflows/`; 门失败回灌 `formatRepairError` 结构化契约 = `[rule:<id>] class=… · 下一步：… · 证据：<原始输出>`，不再回裸终端文本). Config: `preProcRoot` (gate glob root), `adapter` (default alioth-app.yaml), `workflowRoot` (optional override).
- `packages/alioth/verify-alioth/` — **verification/closure library**（原型定位兼容上游构建产物 `Prototypes/Apps/{app}/a-v*.html`（mtime 最新），不再只看 `prototype.html`；`evaluateStageGate` 的 block/service 阶段按上游 `{block}`/`{service}` 模板**按声明集实例化**，`quality` 阶段判 `schema_validity`；**`interactionDeclaration` 只认 canonical `flows`/`workbenchPosts`**（`interactionMode` 不在真门禁的 `CANONICAL_KEYS`，认它等于教模型写一个必判 R1 违规的字段），`block-refinement` 不再 fail 整条管线（上游该 stage `has_human_gate: true`），未声明即由调用方登记 per-App 人工门承压（与 orchestrator 同口径）（纯库，只用 node 内建 + `yaml` 真解析器，无 harness 依赖）: eval-report producer（`buildEvalReport`/`writeEvalReport`；规则维度 `schema_validity`（必填集 = 上游 `evaluate.rs:234-242`：id/code/namespace/name/version/status）+ `prototype_standalone`；缺失即 0 分 + violation，缺失 ≠ 满分）、extension runtime verification（`verifyExtensions`：按 vendored Gateway `ExtensionLoader::load_from_dir` 的五个固定文件名/顶层形状/必需键判「声明是否被引擎装配」；任一 uncovered → `status='degraded'`，degraded ≠ passed；报告带 `artifact_fingerprint` 绑定产物）、closure audit（`artifactFingerprint` = sha256(app.json 字节 + extensions/*.yaml 排序序联)；append-only `AppAgentTraces/closure-audit/{seq}.json`；连续 rejected 达 `ESCALATE_THRESHOLD` 升 escalate）、artifact versioning（`versions/{seq:04}-{hash8}/` + `MANIFEST.json`；回退全有或全无）、two-stage asset patch（提案不写盘 → `confirmed: true` + 基准指纹复核才落盘）、build-eval baseline（`parseEvalCases`/`scoreRun`/`decideRegression`，schema `appagent-build-eval/v1`，证据缺失整轮 degraded）、capability advertisement（六组 tools/skills/gates/autonomy/llm/budget，缺值显式 `unknown(reason)`，组级错误隔离）、deferred store（`DeferredItem` 带 `app`/`namespace` 供跨会话按 App 扫描——**缺省归属 = fail-closed 匹配任意 App**（上游 `deferred.rs:286` 对缺 `app_code` 同判），orchestrator 的 `openDegradedGates` 照此过滤；触发条件三种磁盘可判定式：artifact-exists / artifact-fingerprint / artifact-json-pointer（RFC 6901 深比较，**前导零数组下标判 not-found**——解锁判据不宽松解析）/ artifact-json-pointer-exists（任一命名 pointer 已声明非空——「人工决定待落地」类门的判据，空串/空数组/空对象不算声明）；`sweep()` 全库解除已满足项、对损坏登记文件跳过不炸；`all()` 把损坏/不可读登记**合成为显式未决项**而非跳过——损坏登记 MUST NOT 被当作「无阻塞」，它对任何 App 的 publish 持续阻断直到人工修复）、usage aggregation（按模型 token/调用/墙钟 + 可注入价表的成本估算，无价表明示 `unavailable`）、7 阶段实质判据（`evaluateStageGate`）、publish 影子五谓词（`evaluatePublishShadow`，零行为变更）。
- `packages/alioth/guard-alioth/` — **执行面守卫插件**（`ctx.aliothGuard` service）：`tools/pre-execute` 三条 deny（`[rule:tool-outside-declared-surface]` 拒绝声明工具面之外的调用；`[rule:write-outside-sandbox]` 写沙箱 = `Pre-Proc/{ns}/{Sources,Prototypes,Apps,AppAgentTraces}/` + 拒 `..`/`.` 穿越 + `autonomy.yaml` 写黑名单；`[rule:plan-write-outside-scope]` plan 步写面收窄到该步 `output_glob`）；范围解析 = 观察 `alioth_workflow_*` 参数得 `{ns,app}` + 每次现读 `loadAdapter`/`loadRun`（**无 ns 可解析时不猜**：放行 + warn + `degradations()` 留证）；`tools/post-execute` 修复墙（`RetryBudget` 调用签名 ping-pong + 错误签名修复墙 → `trim-retry`/`escalate`/`terminate` 三档均以 `block` + rule 前缀反馈）；`agent/pre-step` 闭环证据追问（本 turn 调过产物工具但零闭环证据 → 注入一次，上限 1，与澄清/终态互斥）+ turn 预算超限阻断（墙钟 `turnTimeoutSec` / 步数 `maxStepsPerTurn` / 可选成本 `maxTurnCostCents` + `priceTable` JSON 路径；未配价表 → 成本口径 `unavailable` 且**不判**上限，不以 0 冒充）；`session/event` 作为唯一观察面（用量/turn 台账/范围缓存）。`whitelistSource()` 报门禁程序白名单生效来源（`file`/`code_default` + 可区分降级原因）——降级证据不只活在日志里。
- `packages/alioth/tool-alioth-verify/` — **验证/闭环的模型面工具**: `alioth_verify`（artifacts/extensions/stage）、`alioth_closure`（verdict/status）、`alioth_version`（snapshot/list/rollback，rollback 需 `confirmed: true`）、`alioth_patch_assets`（propose/apply，apply 需 `confirmed: true`）、`alioth_capabilities`（六组）、`alioth_deferred`（register/list/unlock）、`alioth_usage`。全部薄封装 `@dsh-alioth/verify-alioth`，零 LLM；扩展形态清单 `allowedForms` 只有一个来源——verify-alioth 的 `EXTENSION_FORMS`（由 `FORM_SPECS` 派生，顺序即 loader 读取顺序），`tool-alioth-verify` 与 orchestrator 都引用它、**不各抄一份字面量**（复检时 orchestrator 的自带拷贝已删）；degraded 扩展验证会登记 per-App deferred 人工门（解除条件 = canonical `extension-verify.json` 的 `/status == passed`），降级运行**删除陈旧 canonical**，故门不会被旧证据自我解除。
- `packages/alioth/bundle-alioth/` — the Alioth plugin group: one bundle (`cordis.patch.yml` via `dsh.bundle.patch`) mounting the full capability set（patch 级另有九处禁用覆写：`directory-picker`，以及右侧栏/文件/终端/本地应用打开的 UI 与**能力**两半——`ui-sidebar-terminal`+`terminal-controller`、`ui-sidebar-files`+`workspace-files`、`ui-sidebar-documentpreview`、`ui-deliverables`、`open-in-app`+`ui-open-in-app`；理由见下） (env + 6 tool/guard plugins + auth trio + persona). Apply: `dsh --profile headless --patch packages/alioth/bundle-alioth/cordis.patch.yml "<task>"`. Structure roles: **plugins** (env-alioth service, tool-alioth, tool-alioth-meta, tool-alioth-workflow, tool-alioth-orchestrator, guard-alioth, tool-alioth-verify, landing-alioth, auth-alioth capability, auth-web-alioth carrier, billing-alioth capability, billing-web-alioth user-center carrier, feedback-web-alioth carrier (over @deepseek-ai/dsh-page-feedback), tool-tool-feedback-alioth tools) + **libraries** (gen-alioth contracts/generators, skill-alioth adapter/validator, verify-alioth verification/closure — consumed by plugins, never mounted). Auth 按能力/载体拆分（harness 规范）: `landing-alioth` = 落地页（`ctx.aliothLanding` service + `/landing` 路由）; `auth-alioth` = 认证能力（`ctx.aliothAuth` + tools/pre-execute / agent/pre-step 守卫，无 HTTP）; `auth-web-alioth` = B/S 载体（:3900 独立服务器 + webServer 同源路由/门禁/cookie + client 面用户徽章）。
- Commands: `pnpm install` / `test` / `test:coverage` / `typecheck` / `lint` / `lint:fix`（oxlint `--fix --fix-suggestions`；`lint` 与 pre-commit hook 均带 `--deny-warnings`，警告即红）+ gate checks `check:strip-only` / `check:vendor` / `check:versions` / `check:dicts` / `check:tree-assembly` (one-shot: `mise run gates`) + AppAgent build-regression runner `pnpm run eval:appagent validate --cases <path>` / `... run --cases <path> [--baseline <p> --gate]`（参数**不加** `--` 分隔符；基准任务集可用 vendored 上游集 `packages/alioth/env-alioth/vendor/Meta/backend/app-agent/eval/cases.yaml`）; `mise run launch` (web GUI, port 3100, `DSH_WEB_PORT`/`DSH_OPEN`), `mise run dev` (headless one-shot; needs `DEEPSEEK_API_KEY`), `mise run alioth:network-tests` (runs the suite with `DSH_ALIOTH_NETWORK_TESTS=1` — needs the local dev DB).
- **内存归因工具（macOS，2026-09-24 新增）**: `pnpm run probe:node-memory`（`scripts/node-memory-probe.ts`，零依赖，`knip.json` 放行 `footprint` 二进制）——主指标是 **charged footprint**（`footprint(1)`，jetsam 真正计数的量），不是 `ps` RSS：实测本机 dev 栈的压缩回收让 RSS 漏掉一个数量级（AliothStudio Gateway vite：**941 MiB footprint / 24 MiB RSS**；codegraph daemon：782 / 136 MiB），任何"按 RSS 算内存"的推理都会低估。超 `--threshold-mb` 时落 `vmmap -summary` + `footprint` 原始证据，并向**已 instrumented** 的 node 发 SIGUSR2 取诊断报告 —— `javascriptHeap.usedMemory`（JS 堆）vs `memoryLimit`（24 GiB 机型 4.19 GiB）vs `externalMemory`/`mallocedMemory`（Buffer/ArrayBuffer/native）直接给出归属，`header.commandLine` 补掉 `JetsamEvent` 不存 argv 的归因盲区。`--launch "<cmd>"` 自动注入 `NODE_OPTIONS="--report-on-signal --report-signal=SIGUSR2 --report-directory=…"`；未 instrumented 的目标**一律不发** SIGUSR2（node 默认把它当终止信号）。推论：`--max-old-space-size` 对堆外膨胀是空操作，硬上限只能是 OS 级（cgroup/launchd/ulimit），且它掩盖问题——先归因，再修机制。
- Profile overlays: `--patch` top-level rows REPLACE existing rows (ids must match the base tree, e.g. `system-prompt` persona); new plugins go in an `insert:` block — rows with unmatched ids are silently ignored. Tree assembly over ALL shipped compositions (bundle headless, example headless, example web) is gated: `pnpm run check:tree-assembly` asserts plugin rows + persona + zero warnings.
- Running the composed profiles needs the workspace packages in DSH's profile fallback: `bash scripts/link-dsh-profiles.sh` (symlinks `~/.dsh/profiles/node_modules/@dsh-alioth/*`, all 19 packages incl. the auth trio + billing duo + feedback duo + verify/guard/verify-tools trio; heal does not remove manual links). The same script also links the harness packages the bundle mounts by loader-entry name that no installation closure carries (`@deepseek-ai/dsh-page-feedback` — the launcher's `resolveModuleFallbackEntries` walks the `apps/cli` closure, and nothing in the harness depends on it); a patch row naming an unlinked package fails the boot loudly (`Cannot find package … imported from ~/.dsh/profiles/web/`), which `check:tree-assembly` cannot catch because `--dump-config` never resolves modules. Package sources must be Node strip-only compatible — no TypeScript parameter properties (`constructor(private …)`), no enums, no runtime namespaces — the dsh loader runs `.ts` entries through Node's native TS strip; ENFORCED by `pnpm run check:strip-only` (TS-AST scan) in CI + pre-commit.
- **Frozen-model positioning (2026-08-18, updated 2026-09-22)**: the model distribution channel is the Alioth model repository — `github:CosmicTools9/Alioth` (or local `../Alioth`), **平铺固定路径** (files at the repo root; the version lives in `latest.json` + the annotated release tag — older releases used versioned dirs `v10.0.x/`, both layouts are accepted by `scripts/generate-semantic-dicts.ts`), MIT licensed (宇器科技 2025). The plugin vendors the consumption-side artifacts (`packages/alioth/env-alioth/vendor/`: the isahl_meta **structure baseline**, skill-adapters, prototype build scripts — Apache-2.0 from the AppCreator distribution) but **no model snapshot**: `modelSource` is required (`builtin` retired 2026-09-24, refused loudly rather than silently falling back) — assemble a deployment source with `mise run alioth:model-source` (release content + sidecar rows + the vendored kit); `--fixture-seed` builds a keyless self-check source. The semantic-mapping library (`skill-alioth/src/data/`) is generated OFFLINE by `scripts/generate-semantic-dicts.ts` from the Alioth repo (coordinates/physical tables) + vendored isahl_meta seed (FK index) — no dev-DB access; rebuild with `ALIOTH_REPO=... node --import tsx scripts/generate-semantic-dicts.ts`. Model evolution = new plugin releases. Pure-consumer rules unchanged; shell reference assets (closed-source-only) replaced by in-repo equivalents (planned).
- Next: in-repo shell reference equivalents (gateway-shell/prototype-base), prototype build gate end-to-end, semantic-model release asset.
- Known LSP noise: `cordis.patch.yml` files report yaml-schema JSONPatch errors + unresolved `!!js` tags — dsh's patch format (id-match rows, insert blocks, JS-tag config) is valid and verified by `--dump-config`; harness has no suppression either. Ignore.
- Pipeline: the complete AppAgent flow is implemented in TS as a deterministic 9-stage machine aligned with the ACTIVE AliothStudio Meta app-agent (NOT the frozen vendor copy — the active line added AppCreation stage 0, E2EVerification retry ≤3, PipelineAdvance gate sweep over the 7 StageIds, PipelineGateAwaiting). 2026-08-25 对齐（remove-appagent-hollow-analysis-stages）: Meta 废弃三分析状态（SemanticAnalysis/FunctionDecomposition/OntologyAnalysis——关键词空壳+透传，legacy passthrough 直跳 Planning，真实分析 = Planning LLM 本体输出）; dsh-alioth 保留三阶段（有真实确定性工作：semantic audit 确认 / registry grounding / 实体注册），管线序 serde wire 兼容。产物契约对齐: module.json status 枚举 = Meta ModuleStatus 全集（active/inactive/beta/planned/developing，draft 由 Meta 归一 planned），生成器默认 category=business/status=planned; extensions 骨架顶层数组（Gateway ExtensionLoader `Vec<T>` 契约，`{}` map 包装会崩启动）。 Contracts: skill-alioth `agent-contract.ts` (serde-alias wire compat), `agent-machine.ts` (pure state machine), `tool-alioth-orchestrator/src/primitives.ts` (real tool bindings through `ctx.tools.execute`; app-creation preflights entity validation to keep PTC atomicity — nothing written before entity checks pass). `alioth_app_create` drives the full pipeline; semantic alignment stays a dialogue precondition (the only LLM seam).
- Pipeline（2026-09-03 对齐，dialog-loop refactor）: Meta 拆除旧会话状态机（orchestrator/state 整拆，会话收敛为对话循环，turn state 由消息推导——业务状态枚举不再存在）。dsh-alioth 保留本地确定性 9 阶段机（PTC 定位不变），合同重锚定到幸存 wire 面：`FlowPlan` 扩展规划字段（semantic_concepts/computations/constraints/business_rules/app_meta/core_constraints）、7 个 StageId 不变、skill Track/Step/Gate schema 对齐（StepGate 增 `expected_exit_code`/`timeout_sec`，Step 增 `reference_paths`/`inputs`，废弃 `outputs` 迁移为 output-glob 门）、GateResult 三态（pass/not-attempted/fail）+ GateErrorKind 四分类（决定 LLM 可修复性）、E2E 阶段产出上游 `e2e-report.json` 证据（orchestrator `preProcRoot` 可选 Config，默认 ALIOTH_PRE_PROC_ROOT 约定）、workflow 桥 step 载荷带 referencePaths/inputs（引擎注入，消除模型探索）、vendored skill-adapters 同步为 dialog-loop 时代 v2 轨道（alioth-ontology/spec-audit adapter 上游已删）。
- **Pipeline（2026-09-13 对齐，AppAgent 大幅改进的吸收）**: vendored 框架整体重同步到 AliothStudio HEAD `1e01d140`（421 文件，从 `git archive HEAD` 净快照同步——上游工作树有 108 个未提交文件，不得静默带入）。新吸收三项：①**门禁脚本可达性**——`check-block-json.ts`（alioth-block 1.1）与 `audit-service-spec.ts`（alioth-service 1.5）此前被 adapter 调用却不在 SYNC_SET，执行即 ENOENT→`path-missing`（不可修复）→track 永久卡死；现已入 SYNC_SET，`scripts/check/baselines` 随目录同步（check-block-json 读其基线），`unreachableGatePrograms()` 在 sync 期与根测试 `tests/vendor-gate-programs.spec.ts` 双重校验（缺脚本 = CI 红，不再等到用户运行时卡死）。②**gate 程序白名单强制**——上游 `RunCommandTool` 按 `_runtime.yaml` allowed_programs 拒绝越界程序（代码常量 `GATE_PROGRAM_WHITELIST` 为底，镜像可追加）；`createProgramRunner({allowedPrograms})` 在 spawn 前拒绝，拒绝文案归 `tool-whitelist`（非 LLM 可修复），workflow 桥传镜像、镜像缺失回退代码常量（镜像缺失绝不放宽面）。③**步骤工具面执行契约**——上游拒绝 `default_tools ∪ step.tools` 之外的调用；harness 侧工具由模型直接调用、部署无法拒收，故 `alioth_workflow_step` 改为暴露解析结果：`harnessTools`（该调哪些具体工具）、`manualTools`（`visual_verify` 无 harness 等价物，走 ego-browser/人工）、`missingTools`（无人满足，显式可见）；映射新增 `run_command→bash`、`code_intel→lsp`。e2e-report v1.0 字段（app/namespace/attempt/checks/failures/note/ts）此前已对齐，无需改。
- **Pipeline（2026-09-22 对齐，AppAgent 契约/执行面/闭环三层吸收）**: 上游自 `1e01d140` 起 app-agent 目录 70 文件 +10.7k 行，7 个 adapter 全漂移（v2.0→v2.2: `default_tools`/`phase`/`inputs`/新工具词汇/新门禁）。本轮吸收：①**门内容谓词** `require_json_pointer`/`require_json_equals`（`skills/mod.rs:85-131`；对 output_glob 命中集 mtime 最新文件取 RFC 6901 值深比较，fail-closed）+ **未知 gate 键即 throw**（不认识的新门禁形态绝不降级为存在性检查）；②**步骤相位** `phase: plan|apply` + 两条加载期不变量（`skills/mod.rs:133-258`）；③**修复契约** `repair.rs`（`RepairClass` + `[rule:<id>]` 信封，取代上游已删除的 `GateErrorKind`；本地失败分类退为 `gates.ts` 内部实现，**不进公共导出、不进模型面**——`GateResult.errorKind` 与 workflow 的 gateResults.errorKind 已删，模型只看到 `repair` 规则码/类别/下一步动作，避免两套分类词汇并存）与**重试预算** `retry_budget.rs`（ping-pong / 修复墙 → terminate > escalate > trim-retry）；④**执行面守卫**（`guard-alioth`：声明工具面 / 写沙箱 / plan 写面收窄，见该包条目）；⑤**闭环验证**（`verify-alioth` + `tool-alioth-verify`：eval-report、扩展 runtime 验证、closure 裁决、版本快照与回退、两段式资产补丁、能力广告、deferred、用量）；⑥**管线实质化**（orchestrator：block 交互形态待决在**骨架落盘那一刻**登记人工门（门扫描晚于 publishing，晚登记=首次发布漏过；判据只认 canonical `flows`/`workbenchPosts`）；模块与 service 之外，**block/service 骨架由管线落盘**（上游同路径 + 已存在不覆写；service 面新增 `services` 声明入口），E2E 的 quality 检查只判产物契约面（`schema_validity`），原型缺失登记 per-App 人工门（幂等、解锁 = 重跑评估该维度为 1）；publishing 前置 3 与 publish 影子同判契约面——原型义务单点落在前置 2 的未决门上，失败原因不重叠；publishing 终局错误带失败前置名与取证；`Config.preProcRoot` **改为必填**——管线按名下驱动工具插件、自己解析产物路径，缺省回退会让「工具配了根、管线没配」的挂载跑到 module-creation 才失败并报出操作者从未配置的目录；模块镜像缺源产物时给**管线本地规则** `pipeline-module-artifact-missing`（class not-fixable，证据含本管线根），不再借用「让模型去产出文件」的步骤门规则；7 阶段门禁换成实质判据、publishing 五项前置 fail-closed（扩展验证未 degraded 且指纹绑定、quality eval-report passed、快照写入、指纹匹配的 approved closure 裁决、无未决降级门）、受控并行原语 `parallel.ts`（共享写面单 owner 串行、并发默认 1、结果确定性排序、失败收敛显式未完成清单）、publish 影子五谓词零行为变更）；⑦**通道**：vendor 同步源随上游迁移（根 `skill-adapters/` → `Meta/backend/app-agent/skill-adapters/`）并补齐新门程序/评测器，新增 adapter **非脚本引用可达性**检查（`scripts/lib/adapter-references.ts`）；⑧**模型发行物布局**：字典生成器同时认 `<repo>/<version>/` 与平铺固定路径。
- **吸收缺口（有据，未做）**: ①`flow-plan.json`：**已落地（2026-09-25）**——有写（`appCreation` 原子落盘，上游同路径）与读（`skill-alioth/src/flow-plan-wire.ts` 的 snake/camel 双态编解码，缺必需键即拒）路径，计划字段不再装饰；扩展产物由计划**确定性派生**（`gen-alioth/src/extension-plan.ts`：constraints/rules/statemachines/workflows/profiles，形状对齐 vendored Gateway 加载器契约，空来源退如实骨架），模型面入口 = `alioth_app_write` 的 `plan` 参数。残余：上游 `request-no-impl/*.md` 缺口文档未产出（缺口现由 per-App 人工门 + `alioth_deferred` 承载）。②gui 1.6 vite build 前端门禁缺可执行环境（provision 不含前端 workspace/package.json，上游靠切 cwd + `pnpm install` 自愈）。③block 1.3/module 1.5 的 `target/debug/ontology-mapping` 门禁：上游运行前自动 `cargo build -p ontology-mapping` 自愈，dsh-alioth 无 Rust 运行时组件，只能如实 `path-missing` 失败。④上游 `auto_gate` 的**自动放行**（影子满 10 次零反向后启用）未启用——本仓只落影子评估，放行仍归人工/审批缝。⑤core_constraints 每轮注入 / ping-pong 连续批语义 / autonomy / retro-EvoAgent 观测——分别属 harness 对话循环与 Meta 后端，消费方不持有该面。⑥vendored `scripts/check/check-shared-kernel.ts` 只扫 `Sources/Services`，新布局（`Sources/Apps/...`）下探测为空：属上游镜像文件，本地补丁会被下一次 `sync:framework` 覆盖，须上游修（同 `prototype-tool.js` 之外的镜像文件一律不改）。⑦上游 `extension_verify` 的 L3「真引擎执行」：本仓以加载器契约的结构化验证 + 指纹绑定替代（无 Rust 运行时），未覆盖面判 `degraded` + 人工门。⑧ **原型面的归属（2026-09-24 定策并落地）**：原型是 workflow 步骤的产物（上游 `stage_config.yaml` 的 `Prototypes/Blocks|Modules/{x}` 模板 + `prototype-tool.js build` 的 `a-v*.html` output_glob，全链无人写 `prototype.html`），不属 PTC 管线职责。故管线各处判据统一为**只判本次 run 自己的产物契约面**（`eval-report` 的 `schema_validity`），原型面由 **per-App 人工门**`prototype-standalone:{ns}/{app}` 承压：解锁条件 = 重跑评估后 `/dimensions/prototype_standalone == 1`（「一次真实通过」解除，不锚文件名），publishing 前置 2 按 App 扫描即阻断发布。实测：预置原型后一次 `alioth_app_create` 全链跑通（`isError=false`）；无原型则在 publish 前置被该门挡住，错误文本带失败取证。⑨ **block 骨架生产已补**（2026-09-24）：`generateBlock`（gen-alioth）按上游 `create_block_scaffold`（`tools.rs`）产出骨架——`block: ""`、`coordinates: null`、`sharing.mode: "single"`、`prototypeVersion: "v1"`，另**多填 `aliothVersion`**（生成期即知模型版本；本仓契约/R2 要求该键，照抄上游省略等于自造一次必失败）；`blockCreation` 落盘到上游同一路径 `Sources/Apps/Blocks/{id}/block.json` 并遵守上游 Y4「已存在不覆写」。契约 `block.schema.json` 由「只要求 id/name」对齐到 check-block-json 的 R2 必填集。残余：交互形态（`interactionMode`/`flows`/`workbenchPosts`）与 `block-refinement` 门仍属模型侧（门照旧 fail-closed）。⑩ **service 骨架生产已补**：`alioth_app_create` 新增 `services` 声明入口（模型面快照已刷新），`serviceApi` 落盘 `Sources/Apps/Services/{id}/service.json` 骨架（同样不覆写）；取值均有实例依据（真实 service.json 5/5 `layer: 1`、`backendCrate = <ns小写>-service-<id>`、`ontology.entities` 可空），未产出面如实置为 `hasBackend: false`（后端 crate 由 service 轨道编写）。残余：上游 `module_design` 还声明 `Prototypes/Modules/{module}/m-v{N}.html`，本仓判据只看 `module.json`（同属原型面）。⑪ **block 交互形态归口人工门（2026-09-24 落地）**：上游把 `block_refinement` 标为 `has_human_gate: true`（问「流程流 vs 工作台」）——形态是**人/模型的决定**，管线只产骨架，故不再因此 fail 整条管线：`blockCreation` 落盘骨架的**同一刻**登记 per-App 人工门 `block-interaction-form:{ns}/{app}:{block}`（时序要紧：7 阶段门扫描在 publishing **之后**，留到那时登记本次发布已放过去）。解锁条件用新触发语汇 `artifact-json-pointer-exists`（`/flows` / `/workbenchPosts` **任一**已声明即解除——「等于某值」表达不了「已声明」，BLOCK_SCHEMA §1.2 两键皆 OPTIONAL、内容由模型定）。判据面同时纠正：`interactionMode` **不在** check-block-json 的 CANONICAL_KEYS（R1 会判违规），旧门却认它——已改为只认 canonical 两键。实测：未决 → create 在 publishing 被门挡住（错误文本点名 `publish-no-open-degraded-gates`）；补 `flows` 后 `sweep()` 自动解除；预声明形态的 block → 一次 create 全链通过且骨架不被覆写。
- **Framework 同步（真相源 = AliothStudio）**: dsh-alioth vendor 的框架代码（design references、prototype 工具链、build/check 脚本、skill-adapters、Framework/backend crates——生成的 Sources path-dep 依赖它们，vendor 后任意部署可 cargo 解析）一律以 AliothStudio 工作仓为真相源，定期 `ALIOTH_STUDIO_ROOT=../AliothStudio pnpm run sync:framework` 同步 + `check:vendor` 判合规（逐文件 sha256 清单 `PROVENANCE.json` 已于 2026-09-24 退役）；`sync:framework --check` 为本地新鲜度门（逐文件 sha256、补丁豁免——本地对 prototype-tool.js 的 PROTOTYPE_TOOL_ROOT 覆写自动重应用且不计漂移）。**skill-adapters 的源路径（2026-09-22）**：上游把 adapter 从仓库根迁入 `Meta/backend/app-agent/skill-adapters/`（根目录已删）——SYNC_SET 必须随之改源，否则同步直接 `source missing` 失败（那次迁移当时静默地让冻结链失效）。同轮补齐的新条目按"adapter 门禁/工具链真实引用"逐个入集：`scripts/capability-catalog.ts`、`scripts/check/check-prototype-{types,render}.ts`、`scripts/check/check-namespace-frontend.{sh,ts}`、`scripts/eval/appagent-build-eval.{ts,selftest.ts}`、`scripts/eval/appagent-client.ts`、`Meta/backend/app-agent/eval/cases.yaml`（保持上游 repo-relative 路径——vendored 的上游 runner 按该路径解析）、`docs/specs/BLOCK_SCHEMA.md`/`MODULE_SPEC.md`、`.agents/skills/alioth-service/references`。可达性双重校验：`unreachableGatePrograms()`（门禁**脚本**）+ `scripts/lib/adapter-references.ts` 的 `unreachableAdapterReferences()`（adapter 的 `reference_paths`/`inputs` 等**非脚本**引用，模板段按通配），sync 期与 `--check` 期都跑，`tests/vendor-adapter-refs.spec.ts` 在 CI 兜底。上游工作树脏时（`git status` 非空）改用净快照作真相源：`git archive HEAD | tar -x -C /tmp/alioth-head` 后 `ALIOTH_STUDIO_ROOT=/tmp/alioth-head`，未提交的上游改动不得静默流入。CI 不跑此门（真相源在工作仓，不在公共克隆）。
- **Stack alignment with AliothStudio (verified live)**: PostgreSQL 18.6 — every environment runs a real server: host PG 18.6 on dev/m2/prod, the PGDG build inside the container, `postgres:18` as the CI service. env-alioth never provisions a cluster, so `ALIOTH_DATABASE_URL` is required everywhere (unset ⇒ boot fails loud). Node 24.20 (`node:24.20-slim`), bun 1.4.2 (pinned), Rust 1.96/edition 2021 (declared; dsh-alioth has no Rust runtime components — prototype gates are the bun scripts).
- **Host toolchain single-sourcing (2026-08-20, updated 2026-08-29, 2026-09-06; environment-consistency policy)**: repo `.mise.toml` pins `node = "24.20.0"` — overriding the global mise default so host dev matches the container image exactly; `mise install` in the repo is the one-command rebuild. pnpm follows the **deepseek-harness** line: `packageManager: pnpm@12.3.4` (same as the harness repo, corepack in Docker/CI resolves it per-package), deliberately replacing the former AliothStudio-matching 11.24.0 pin so the consumer workspace and its harness source tree share one toolchain. Do NOT add pnpm via this repo's mise config — packageManager + corepack is the single source.


- **Host harness source dependency (2026-09-04; refreshed 2026-09-24; no npm harness releases)**: `@deepseek-ai/*` devDependencies pin `^0.1.7-rc.1` (the Alioth line: official upstream + Alioth Developer local commits) and resolve to the sibling `../deepseek-harness` checkout through `pnpm-workspace.yaml` (same pattern as the harness workspace itself including `../dsh-chess`). **The declared range is the selector for that checkout**: `linkWorkspacePackages: true` links the workspace member only while the range satisfies the local package version — otherwise pnpm falls back to the registry, which carries no harness releases, and the install fails. Advance the ranges together with the checkout in one commit (d8cf821 for `0.1.6-alpha.2`, this change for `0.1.7-rc.1`), drop declarations for packages upstream removed (`dsh-settings-file` was absorbed by `dsh-settings` in `#4587`), and regenerate the lockfile: a **frozen** install skips resolution and therefore hides an out-of-date range until the next manifest edit, while `pnpm run <script>` refuses outright on a lock/manifest mismatch. The checkout must be built first (`pnpm run build:lib:host && pnpm run build:lib:client` in deepseek-harness; watch for stray no-`package.json` dirs under `packages/*/*` that make tsdown read the root manifest and fail entry resolution, and for the unapproved `@deepseek-ai/dsh-subprocess-local` build script — approve it in the harness `pnpm-workspace.yaml`). CI/Docker clone the harness at the leading baseline and build it before installing this workspace. Deploy note (2026-09-09): the 0.1.5 native/system package needs `native/system/packages/darwin-arm64/bin/system.node` materialized — the file is git-tracked but `*.node` is gitignored, so shallow/bundle transfers can drop it; check it exists after every harness sync or agent rounds crash with `Cannot find module .../bin/system.node` (flock.js). Without the sibling checkout, `pnpm install` fails (the pinned version is not on npm) — that is intentional: adapt against source, never against an older registry line.
- Docker delivery: the image builds the harness **client (web) face** too (`scripts/build-harness-web.ts` → `apps/web/dist` + the client build record, shipped as `.dsh-build/`), because a lib-only build leaves the console unable to load plugins while `/landing` still answers 200 — `scripts/docker-check.sh` asserts both artifacts. `Dockerfile` ships the group as a runnable container (node:24.20-slim + bun + PGDG PostgreSQL 18 + no model snapshot of its own — a real run mounts an assembled source, `--check` assembles a fixture one; non-root `USER node`). Build `docker build -t dsh-alioth .`; run `docker run --rm -p 3100:3100 -e DEEPSEEK_API_KEY=... -v alioth-data:/data dsh-alioth`; keyless self-check `docker run --rm dsh-alioth --check` — the entry point starts PostgreSQL 18 on `/data` and exports `ALIOTH_DATABASE_URL` before the check runs (composition smoke + doctor, the latter with `--allow-unbuilt-semantic-index`: a fresh `/data` has no semantic index and rebuilds it on first search, while every other check still gates). Docker build + self-check run as a CI job (`.github/workflows/docker.yml`).
- Verification matrix: (0) CI gate matrix (`.github/workflows/ci.yml`) — typecheck (packages+scripts+tests) · lint · tests + coverage thresholds (80/70/80/80 ratchet) · knip · strip-only · vendor provenance · version sync · vendor compliance (LICENSE/NOTICE only since 2026-09-24) · dict freshness (clones the Alioth repo; fk-index there degrades to anchored bytes, no sidecar) · tree assembly · composition smoke · commitlint · audit (moderate floor; overrides in `pnpm-workspace.yaml`) · shellcheck · gitleaks; (0b) L2 agent auto-repair (`.github/workflows/agent-fix.yml`) — on `ci` failure for same-repo PRs: L0 `lint:fix` → dsh headless agent 修复残余语义错误（prompt 带失败日志，禁止改门禁/CI 配置）→ 本地 quick gates 验证 → 推送修复 commit 回 PR 分支（`[agent-fix]` 标记，每分支上限 3 次；需 repo secret `DEEPSEEK_API_KEY`，缺失则 no-op）； (1) per-plugin unit/integration — `pnpm run test` (738 tests incl. 1 skipped, real PostgreSQL 18 — per-suite throwaway databases; branch coverage 81.6% — floor 70; network-gated behind `DSH_ALIOTH_NETWORK_TESTS=1`, run via `mise run alioth:network-tests`); (1b) model-visible surface keyless snapshot — `tests/model-surface.spec.ts` pins all 26 tool JSON schemas to `tests/__snapshots__/model-surface.json` (refresh: `UPDATE_SNAPSHOTS=1 pnpm exec vitest run tests/model-surface.spec.ts`); (1c) AppAgent 构建回归基线 — `pnpm run eval:appagent validate|run --cases <path> [--baseline <p> --gate]`（消费侧跑者：从 `extension-verify.json`/`e2e-report.json`/`AppAgentTraces/closure-audit/*.json`/`eval-report.json`/产物清单取机械证据 → `scoreRun`；证据缺失整轮 degraded；`--gate` 是唯一非零退出模式，缺省只报告不改态；vendored 上游集 `packages/alioth/env-alioth/vendor/Meta/backend/app-agent/eval/cases.yaml` 可直接 validate）; (2) composition — `node --import tsx scripts/smoke-composition.ts` mounts the full group on a real Context: 26 tools registered, assembled fixture model source ready (zero network), schema_info round-trip, doctor core green（`ALIOTH_DATABASE_URL` 必需；本机若指向没装注册表的库会红，CI 形态是把 `ALIOTH_DATABASE_URL` 指到 `postgres:18` service 建的 `dsh_alioth_ci`）; (3) tree assembly — `pnpm run check:tree-assembly` runs `--dump-config` over ALL shipped compositions (0 warnings, rows + persona asserted); (4) real dialogue e2e — `mise run dev` with a real key (semantic → entity → app → workflow → inspect); (4b) 浏览器端到端（部署面）— 一律用 `ego-browser` 技能：打开 `/?token=…`、复用已登录会话、驱动真实 UI（认证门禁 / 工作区选择 / 对话与工具调用），断言 DOM 状态 + 截图留证；**禁止 Playwright**； (5) manual acceptance items — prototype build chain (bun), AliothStudio import, web-profile approval.

### Licensing

- Apache-2.0, following AppCreator's license (`LICENSE`, `NOTICE`; every manifest declares `"license": "Apache-2.0"` — gated by `pnpm run check:versions` together with single-version sync across all 19 packages).
- Runs on the DeepSeek Harness as a dependency — MIT, Copyright (c) 2026 DeepSeek — and the container image **bundles a built harness checkout**, so the image redistributes MIT (harness) alongside Apache-2.0 (plugin group + vendored model artifacts). The image therefore carries `/app/{LICENSE,NOTICE,THIRD_PARTY_NOTICES.md}` and `/deepseek-harness/{LICENSE,THIRD_PARTY_NOTICES.md}`: `scripts/docker-check.sh` asserts them inside the container (runs in the `docker` CI job) and `check:versions` asserts the three root files exist for the source distribution — a redistribution that drops them fails CI, never silently. Aggregate third-party disclosure = `THIRD_PARTY_NOTICES.md`. **Commercial use additionally requires authorization from this project's operator** (a contractual matter with the copyright holders; it does not change the permissive file-level licenses, which stay accurate as declared).
- dsh-alioth is a sibling consumer of the Alioth model (as is AppCreator); never consumes AppCreator's products. Pulls model artifacts only — `isahl_meta` baseline, skill-adapters, version anchor; the `*isahl_meta*` filename filter excludes the rest.
- Model distribution: `github:CosmicTools9/Alioth` (public, verified reachable; cloned by the CI dicts-freshness job); do not re-route to the internal AliothStudio origin (no LICENSE).
- Vendored artifacts (`env-alioth/vendor/`) ship with in-tree `LICENSE` + `NOTICE` (The Alioth Authors, Apache-2.0) — gated by `pnpm run check:vendor`（2026-09-24 起**只判合规文件在位**：逐文件 sha256 清单 `PROVENANCE.json` 已随 `builtin` 退役——它把本就不提交的派生文件记进账本，且 `check-vendor --update` 已作废；vendor 内容与上游的新鲜度另由本地 `sync:framework --check` 判，不进 CI）。Upstream gaps (public tree missing LICENSE): authoritative Apache-2.0 prevails; raise upstream.

### Repo conventions (enforced)

- **Pre-commit (lefthook)**: staged-file oxlint（`--fix --fix-suggestions` 可修复项自动落地并由 `stage_fixed` 重暂存；`--deny-warnings` 使修复后残留的警告/错误 fail commit）+ strip-only + versions + vendor checks; `pnpm install` auto-installs hooks (`prepare` script — skips cleanly in git-less build stages like the Docker build stage). 手动全量修复：`pnpm run lint:fix`。Typecheck/tests stay in CI + `mise run gates`.
- **Registry**: `.npmrc` pins `registry=https://registry.npmjs.org/` (the scoped mirrors lag behind on fresh beta packages); CI runners default to npmjs anyway. `engine-strict=false` explicit.
- **Typecheck scope**: `tsconfig.json` covers `packages/**/src`, `packages/**/tests`, `scripts`, root `tests` — gate scripts are compiled code too.
- **Coverage floor** (vitest v8): statements/functions/lines 80, branches 70 — ratchet up only.
- **Dependency hygiene**: knip (config `knip.json`) + frozen-lockfile CI installs + `pnpm audit --audit-level=moderate` + security floors as `overrides` in `pnpm-workspace.yaml` (adm-zip ≥0.6.0, sharp ≥0.35.0, yaml ≥2.8.3).
- **E2E in the browser is `ego-browser` — Playwright is banned**: no dependency, no import, no test or script here may use it. End-to-end verification of the deployment (routes, auth gate, workspace picker, chat turns, tool calls) runs through the `ego-browser` skill: drive the real UI, assert DOM state with `page.evaluate`, and keep a screenshot as the artifact. The harness keeps its own Playwright suites upstream (92 files, ~28k lines across apps/web, benchmarks and experimental/inspector); nothing here runs, typechecks or heals them, and no realign/heal rule will be added for their types (the cross-store type-identity failure this once caused on the harness line — a consumer install repointing the harness checker's links at this workspace's store — is closed on that side instead, by a `tsconfig.base.json` `paths` pin: harness Agent Note `2026-09-14-playwright-type-resolution-pin`). The lockfile's transitive `playwright` entries are a consequence of the harness packages being workspace members; `allowBuilds` deliberately omits playwright, so no browser binaries are ever downloaded.
- **Docs**: README is the entry point; CHANGELOG.md records releases; PR/issue templates + CODEOWNERS under `.github/`. AGENTS.md numbers (test counts, dict sizes) must be refreshed in the same PR that changes them.

## Architecture principles

- **B/S 交付与用户隔离（auth 三插件：landing-alioth / auth-alioth / auth-web-alioth）**: the plugin group ships as a browser-accessible B/S deployment, decomposed per the harness capability/carrier norm — landing-alioth (落地页能力: `ctx.aliothLanding` + `/landing`；同时**托管 origin 的品牌资产**——`/favicon.svg`、`/favicon.ico`（16+32 双尺寸 PNG-ICO）、`/apple-touch-icon.png`，因为同一 origin 上每份文档都要用它们：我们的页面用声明，harness 控制台 shell 用约定路径 `./favicon.svg`；**不发布 web manifest**——两条约定路径 `/manifest.webmanifest`（harness 控制台 dist 自带 `DeepSeek Harness`/`display:fullscreen`）与 `/site.webmanifest` 一律 404，manifest 被删（旧文件已随改动删除）：任一可被浏览器读取的 manifest 都会把 origin 上的文档变成「可安装为本地应用」，而产品是 B/S 控制台；我们的页面也全部摘掉 `<link rel="manifest">`，只留图标集), auth-alioth (认证能力: `ctx.aliothAuth`, scrypt passwords, bearer sessions in `dsh_alioth_auth` — SEPARATE from the registry so `resetRegistry()` never wipes users; `tools/pre-execute` namespace guard + `agent/pre-step` enforce guard; **工作区能力**: 模式判定 `ALIOTH_WORKSPACE_MODE` > Config.workspaceMode > 默认 standard——只有 `unlimited` 放开「自定义工作区」（workspaces() 对每个用户列出全部 namespace + 路径），standard 固定用户自己的 namespace；**会话绑定改为服务端（2026-09-25）**：身份不再依赖浏览器脚本——客户端嗅探的是 harness 旧 REST 路径，harness 换传输后绑定静默失效，同一 m2 实测会话落到别的命名空间（工具守卫与工作区选择器双双退到路径推断）。现在 `auth-alioth` 订阅 `session/created`，用 harness 的 `currentConnectionAccount()`（web profile 专属包，动态导入，headless 缺包即「无账户」）把会话绑到 `dsh_alioth_auth.session_bindings`；`userForSessionId` 先查它，守卫在拒绝前还会补绑一次。客户端脚本降级为冗余路径。 **连接账户隔离（2026-09-09，harness 本地 commit 01817a69a7 + auth-web resolver）**: auth-web-alioth 在 webServer inject 回调内向 harness connection 注册 account resolver（HttpOnly `alioth_session` cookie → 用户 namespace；connection service 异步 provide，注册带 200ms 重试 ≤30s）；harness 侧每个 HTTP `/api` dispatch 与每条 remote.mux 流都在 AsyncLocalStorage 携带该账户（`currentConnectionAccount()`）。app-picker（browse 后端）有账户时只服务该账户的 namespace（静态 `namespace` 锁降级为匿名回退、永不覆盖调用者身份），跨 namespace 路径显式拒绝——多用户实例下**严格不可见**（另一个用户连目录名都列不出），会话只能落在自己 ns，workspace-path 身份推导无冒用面。会话级 bind/guard 仍为执行层兜底。注册时无条件自动为用户创建同名 namespace 工作区（AliothStudio 路径结构 `Pre-Proc/{namespace}/` + `Deploy/{namespace}/`，roots 默认 `ALIOTH_PRE_PROC_ROOT`/`ALIOTH_DEPLOY_ROOT` ?? `~/.dsh-alioth/{Pre-Proc,Deploy}`——部署自有目录，**绝不默认牵扯 AliothStudio checkout**（要往 AliothStudio 写产物必须显式设 `ALIOTH_PRE_PROC_ROOT`）；`ctx.aliothAuth.ensureWorkspace/createWorkspace/workspaces` 幂等 + createWorkspace 仅 unlimited 开放（U- 前缀保留给用户工作区），自动建 `Pre-Proc/{ns}/`+`Deploy/{ns}/` + 命名空间模式防穿越，standard 下 user 只见自己 namespace、admin 跨全部；`appForSession(sessionId)` 由 harness workspace registry 反解会话所属应用工作区（`{namespace, code, dir}`）——只答「在哪」，不答「能否看」，授权归调用方）；**工作区 = 应用**（控制台目录选择器与「应用」视图同源，列的都是 `Pre-Proc/{ns}/Apps/*`）：`ensureWorkspace` 额外预置 **`Apps/default`**（新账号开箱可用，不再空选择器卡住）；载体「应用」页与 `/api/alioth/apps{,/rename}` 可**新建应用**与**原地改名**——两处都只接受**单段名**（与 app.json `code` 同规则），所以跨层级移动结构上不可达（`/api/alioth` 刻意独立于 harness 的 `/api/workspace/*` RPC 命名空间）；`app-picker` 的 browse `createDirectory` 委托同一 `ctx.aliothAuth.createApp`，于是控制台的目录流程（入口文案「选择工作区」——创建与改名归应用管理页）与「新建应用」同源；**用户名不可改**（用户名 ⟺ namespace `U-<username>`，公开服务面没有改名路径）), auth-web-alioth (载体: `POST /api/auth/register|login|logout|bind`, `GET /api/auth/me`（含 `workspaceMode` 供客户端徽章切换入口）, `GET /api/workspace` + `/workspace` 页面（unlimited 呈现「工作区」——列出全部 namespace（AppAgent 档）；standard 呈现「应用」——锁当前用户 namespace 的应用列表（AppCreator 档，人人平等、无超管）; standalone :3900 server + webServer 同源路由; login sets HttpOnly `alioth_session` + marker `alioth_user` cookies; 徽章 chip 带「工作区/应用」入口). **右侧栏（harness 0.1.7 新增，产品面只开原型）**：控制台只暴露我自己注册的两个 tab 类型——`@dsh-alioth/sidebar-prototype`（guide 入口「原型」）与 `@dsh-alioth/sidebar-alioth`（「应用状态」）——外加停靠面 `ui-sidebar-right`。**禁用的是能力，不只是 UI**：`ui-sidebar-terminal`+`terminal-controller`（上游 Web 终端以 subprocess provider 的系统用户身份直接执行，不走 Agent 沙箱、不要审批——Agent Note `2026-09-16-user-terminal-permissions` 自陈「坐在浏览器前的人即机器主人」；B/S 多租户下等于把服务账号 shell 开给任何注册用户）、`ui-sidebar-files`+`workspace-files`、`ui-sidebar-documentpreview`、`ui-deliverables`、`open-in-app`+`ui-open-in-app`（宿主侧三个 webServer 路由中 `POST /open-in-app/open` 会以服务账号在**部署机**上启动探测到的桌面应用，目录由调用方给绝对路径——远端的「本地应用」不是浏览器用户的机器，同终端那条理由）——`workspace-files` 自己的契约写着「files outside it are allowed」，即绝对路径任意可读，只摘 tab 挡不住 Remote 直连。**可见性只有一条真相源**：`packages/alioth/auth-web-alioth/src/prototypes.ts` 的 `isPrototypePath()`——只放行命名空间级 `Pre-Proc/{ns}/Prototypes/**` 与 AppCreator 布局 `Apps/{app}/prototype.html`（及 `Apps/{app}/Prototypes/**`），且 `listPrototypes()` 跳过符号链接（原型树不得成为望向部署其余部分的窗口）；`GET /preview/<rel>`（字节面）与 `GET /api/alioth/prototypes?sessionId=`（清单面：cookie 鉴权 + 会话→应用由 harness workspace registry 反解 + 命名空间比对授权，越界 403）共用它，源码/`app.json`/extensions/轨迹一律 404（对本人也是）。**源码 = 付费限时下载（两层时效）**：控制台没有任何源码读取路径，下载走两段式。① `POST /api/alioth/source/request`：cookie 鉴权 → 会话→应用反解 → 命名空间比对 → **权益判定 = L2 授权**（`ctx.aliothBilling.sourceLicense`，`now <= until` 即可下；**L1 订阅不解锁**；缺 billing 能力则 fail-closed）→ 签发**绑定账号**的短时链接；无授权/授权过期 → **402** + `licenseUrl`（指向用户中心价格阶梯）。② `GET /api/alioth/source/download?token=`：验签 → 过期 → **账号绑定**（转发的链接对别人无效）→ **再校验授权**（授权期内签发的链接不得跨过授权末）→ 打包 `app.json`+`prototype.html`+`modules/`+`extensions/`+`Sources/`（prune `target|node_modules|dist|.git`、跳过符号链接、64 MiB 上限**失败即拒**不截断）→ store-only zip（零依赖自研，`zip-store.ts`）+ 一行 `source-downloads.jsonl` 审计。链接密钥 `<dataRoot>/source-signing.key`（0600，首次自建，跨重启稳定）；TTL 取 Config `sourceLinkTtlSeconds`（默认 900s）。客户端在「原型」tab 内申请并跟进，402 时提示「源码下载需 L2 授权（¥4,999 起，商务对接）」并给「查看 L2 授权」入口。「应用状态」tab 由 `GET /api/alioth/app-status?sessionId=` 供数，只读投影产物契约校验 / 扩展装配验证 / 流水线 run 位置 / 未决门 / 闭环裁决 / **模型依赖**（面板「模型版本」= 本部署模型，经 `ctx.aliothEnv.modelInfo()` 读取——只解析模型源、不碰数据库；「模型依赖」= `app.json.min_alioth_version` 是否被满足，任一侧不可判定即报不满足；判定在服务端算好，客户端只画）——**只读是硬约束**：不 `sweep()` 未决门（解锁归工具）、不 `loadRun`（缺文件会写 fresh run，故 skill-alioth 新增 `readRun`）。Each user owns an Alioth namespace `U-<username>` (no super-admin; every registered user is equal — no super-admin exists (workspaces/guard lock every account to its own `U-<username>` namespace)). `mode: 'enforce'` requires an authenticated session for every namespace-scoped call (deployment override `ALIOTH_AUTH_MODE=enforce` for B/S production); `open` keeps headless deployments working. **反代部署的公网 origin**: 进程绑 loopback（`--host 127.0.0.1`），网关在别的端口/权威上终结时用 `ALIOTH_PUBLIC_ORIGIN`（或 config `publicOrigin`）显式声明浏览器实际到达的 origin——控制台 token 交接 URL 与工作区跳转链接都取它（缺省才回落到「请求 Host + 绑定端口」的直连形态；值非裸 http(s) origin 时挂载即失败）。否则登录/注册会 302 到 `http://<域名>:3100/` 这类外网不可达地址。**备案号页脚**：landing / auth 页 / 用户中心三个载体各有 config `icp`——**config-only（插件内不读全局 env）**，否则一个全局 env 会越过「按面上号」的控制权；由 bundle 装配把 `ALIOTH_ICP` 注入需要的面（本部署**只上首页页脚**），渲染为链到 `https://beian.miit.gov.cn/` 的页脚；未配置则**不渲染**（自托管副本不继承他人备案号），配置值经 HTML 转义。Browser form posts (urlencoded) get styled HTML; JSON clients get JSON. Single shared workspace: isolation is namespace-level, not filesystem-level. **Web gate（:3100 登录拦截）**: auth-web-alioth 经 `ctx.inject(['webServer'])` 延迟挂载同源认证面（`/login`、`/register`、prefix `/api/auth/*`，longest-prefix-wins 胜过 client-connection 的 `/api`）+ tapIndex 门禁脚本（无 `alioth_user` cookie → `location.replace(aliothLanding.path)`；包裹 fetch 嗅探 `/api/sessions.create` 响应 → `POST /api/auth/bind` 完成 session 绑定——harness API 是 in-process，HTTP 身份到不了 tool 执行，只有 session binding 携带身份）。`agent/pre-step`（enforce）reject 未绑定 session 的步。Config `webGate: false` 关闭挂载（默认 true；headless 无 webServer 自动不生效）。**Client 面**: `auth-web-alioth/lib/client.js` 手写 closure-factory 工件（harness clientBundle 预设未发布，本模块零打包），经 `dsh.client` 声明组进 `__DSH_BOOT__`；用户徽章注册进 `shell.overlay` list 槽（必须 `ctx.slots.inject` 延迟注册，直接 register 与 ui-layout 声明赛跑）。**本地独占的控制台维护面（设置面板 + 插件面板）**：harness 的 Settings（模型/内置插件/Agent 预设）读写的是服务宿主自己的配置、其富动作本身也只在 loopback 渲染；插件面板（`ui-plugin-manager`，侧栏入口 id 与 `main` key 同为 `plugins`）安装/启停宿主插件行——两者都不是多租户浏览器的事，故非 loopback authority（`localhost`/`[::1]`/`127/8`——与 harness `/api` Host fence 同一判据）下以 priority -1 占位遮蔽：`sidebar.settings`（single 槽渲染最低优先级的活条目）与 `main` key `plugins` 渲染 null；`sidebar.panellist` 的 `plugins` 格额外**隐藏自身所在的行**——侧栏的行外壳（按钮/标签/提示）由 ui-sidebar 绘制、单元格只供图标，空占位会留下一个空行；该隐藏 fail-open（行不再位于 `button` 内即不隐藏，靠浏览器 E2E 钉住）。loopback 上不注册、harness 原样生效。判据优先取 `ctx.get('connection')?.isLoopback`，服务未挂载时回落页面 authority，两者都拿不到即隐藏（fail-closed）。`.gitignore` 对该 lib/ 有例外。
- **用户中心（billing 双插件：billing-alioth / billing-web-alioth）**: 订阅（L0/L1）、账单（按月生成）、发票（自助：申请即开具——无管理员审核，2026-09-05 去超管后）按能力/载体拆分 — billing-alioth 定义 `ctx.aliothBilling` 契约 + **过渡内存实现**（用户决策 2026-08-21：不做 DB 建模，正式计费后端在同名接口后替换 provider；支付无外部渠道，账单 unpaid→paid 走明示的线下确认）；billing-web-alioth 挂 `/usercenter{,/subscription,/bills,/invoices}` 页面 + `/api/billing/overview|subscribe|cancel|pay|invoice|issue`（cookie 鉴权，未登录弹 /login；表单流 302 回对应 tab 带 notice/error）。入口：徽章「用户中心」链接。定价常量 L1=139900 分（¥1,399/月）。**源码下载权益 = L2 源码下载授权**（价格阶梯原文：L2 ¥4,999 起、由商务对接，**无自助通道**；L1 订阅（¥1,399/月）不解锁源码）。开通路径：用户在用户中心点「申请 L2 授权」（`POST /api/billing/source` → 落一行请求，幂等、不覆盖已开通窗口），运营侧**一条命令**开通/续期：`pnpm run source:grant --user <用户名> --until YYYY-MM-DD [--note 备注]`（`--list` 看待开通清单）——命令直接连 `ALIOTH_DATABASE_URL`，不拉模型快照。授权落 `dsh_alioth_billing.source_licenses`（billing 自己的 schema，幂等 DDL 首次使用时建；`requested_at`/`granted_at`/`until`/`note`）。运营侧 env 清单 `ALIOTH_SOURCE_LICENSES="username:YYYY-MM-DD,…"`（Config `sourceLicenses`，日期含当天末日；格式错**挂载即失败**）保留为**上线前临时开关**，优先级低于落库授权。判定在载体侧（`sourceEntitlement`，只接受授权、**读不到订阅**——门禁拿不到 `getSubscription` 是设计声明）；库不可达时**报错而不静默当无授权**（付费用户不该被一次 DB 抖动判成未授权）。
- **页面批注闭环（feedback 载体+工具两插件；能力来自 harness @deepseek-ai/dsh-page-feedback，移植 AliothStudio scripts/feedback 协议）**: page-feedback（harness 通用包：`ctx.pageFeedback` 批注存储 + pending⇄acknowledged→resolved/dismissed 状态机 + 审计事件链 + long-poll watch + ego-browser 验证缝；node:sqlite，由 bundle 挂载）；feedback-web-alioth（载体：独立 HTTP :14747 回环——CORS allowlist 浏览器写入、loopback 消费者端点、`/feedback` 书签页 + overlay.js 注入，Alt+点击元素留言，经 ctx.pageFeedback）；tool-feedback-alioth（模型面工具 `alioth_feedback_pending|ack|resolve|dismiss` 消费闭环）。信任边界同原作：写入需 allowlist Origin，`Origin:null` 需 `allowNullOrigin` opt-in。同机与 AliothStudio 自家 dev server 共存：bundle 钉 14748。
- **程序化生成优先（alioth-* 技能硬性规范；2026-09-03 修订——全栈面）**: 契约产物（app.json/module.json/extensions/entity 行）一律程序化生成（gen-alioth 生成器 + 契约门工具 `alioth_app_write` / `alioth_app_configure` / `alioth_entity_write`），NEVER free-text LLM 生成——LLM 对契约只供结构化参数与语义决策。**`Sources/` 与 `Prototypes/` 代码文件是明示例外**：模型在 workflow 步骤白名单内用 harness `write` 编写代码（`write_file` 映射到 write 面），由程序化门禁验收（bun prototype build / nav 校验 / cargo check）——门禁失败即拒绝，不经映射层拦截。LLM seams 共两个：semantic alignment（semantic_search hits → model decision）+ gated code authoring（步骤白名单内的代码编写）。Text prompt → LLM → **契约**产物 content 仍是 spec violation。
- **Deterministic main pipeline, zero LLM inside**: track/step state machine, gate checks, artifact generation (gen-alioth), entity validation and registration (entity-validate / alioth_entity_write), and semantic retrieval are deterministic code — no LLM calls inside them. LLM involvement lives harness-side: dialogue driving tool calls, semantic alignment, and gated code authoring in workflow steps (see the amended 程序化生成优先 rule).
- **Semantic alignment served by embedding first**: `alioth_schema_semantic_search` covers synonyms + cross-language deterministically (transformers.js + Xenova/bge-small-zh-v1.5, 512 dims); the model decides on top of the hits. Complex multi-concept alignment may use LLM reasoning — that is the sanctioned LLM seam.
- **Semantic space is a maintainable artifact**: index cached under `<dataRoot>/semantic/`, auto-invalidated by entries hash; explicit rebuild via `mise run alioth:rebuild-semantic` (force re-embed; ~11s for the seeded registry). Model upgrades (resetRegistry → re-bootstrap) change entries → next search auto-rebuilds. Inference backend stays onnxruntime-node CPU (XNNPACK) — decided, no ANE/CoreML work (bge-small is fast enough at 3ms/entry batch; ANE would need a Python service + static-fp16 CoreML conversion; if the model grows to large/m3 or search becomes hot-path, revisit with the omp-ane-embedding route). The semantic library = dict snapshots (in-repo) + model files (release asset, offline via `DSH_EMBEDDING_MODEL` local path; default hf-mirror download; no-model degrades to literal search).
- **PTC (Programmatic Tool Calling) landing**: `alioth_app_create` (@dsh-alioth/tool-alioth-orchestrator) is the programmatic pipeline — fixed sequence (validate → entity_write → app_write → inspect verify), every step through `ctx.tools.execute` so approvals/gates/session log apply identically to model calls. Semantic alignment is a PRE-condition done in dialogue (semantic_search + model decision) and passed in as parameters; the orchestrator makes zero LLM calls. Failure is atomic: a failed entity write aborts before any artifact is written. Optional `Config.adapter` stitches the AppAgent workflow gate into the pipeline (create → first-step gate; gate failure keeps artifacts, re-run workflow_complete after fixing).
- **Operational boundaries (known, documented)**: (1) approval — `approvalMode: required` needs a composed ApprovalService with a UI answerer (web profile); headless deployments must use `bypass`. (2) program gates — the bun prototype-tool build chain is a manual acceptance item (needs the real model tree + concrete artifacts); automated tests cover the runner mechanics only. (3) external DB access — `probe-semantic.ts` / `generate-dicts.sh` need a local dev DB (`DATABASE_URL` / first arg override, default `postgres://isahl@localhost/aliothstudio_dev`).

## Development Commands

### Harness (run in `deepseek-harness`)

```sh
pnpm install                  # pnpm workspaces; Node ^22.19 || >=24
pnpm run test                 # vitest unit tests
pnpm run test:coverage        # CI coverage gate: per-file 100% on packages/*/*/src
pnpm run test:e2e             # real-API tests; self-skip without DEEPSEEK_API_KEY
pnpm run test:snapshot        # keyless ACP/headless replay vs expected outputs
pnpm run typecheck            # tsc -b host + client faces
pnpm run lint                 # oxlint
pnpm run build                # tsc emits lib/, tsdown bundles runtime
pnpm run hygiene              # knip + publint + constraints + invariants
pnpm run check:all            # all repo gates (scripts/run-gates.ts)
pnpm dsh --profile headless "task"   # run one task from source (needs key)
pnpm run mock:llm             # local LLM mock server (keyless dev)
```

### Alioth (run in `AliothStudio`)

```sh
bash scripts/db/reset-db.sh --dev
cd Meta/backend && mise run dev        # Meta backend; frontend: cd Meta/frontend && mise run dev
cd Gateway/backend && mise run dev     # Gateway backend; frontend likewise
bash scripts/test-all.sh               # or: mise test — full test suite
cd Meta/backend && mise run schema-info -- list-tables
cd Meta/backend && mise run schema-info -- describe-table <t>
cd Meta/backend && mise run schema-info -- relations <t>      # also: leafs-of, parents-of, resolve-coordinate --dim <scene|factor|function>
```

## Code Conventions & Common Patterns

### Harness

- Every package is `@deepseek-ai/dsh-<name>`; `@deepseek-ai/cordis` is a peerDependency of every package; **ESM everywhere** (`"type": "module"`), `.ts` in local relative imports.
- Plugin anatomy — copy `packages/todo/tool-todo/`:

```ts
export const name = 'tool-<x>'
export const inject = ['tools']
export interface Config { ... }                    // validated deployment choices
export const Config = z.object({ ... })            // schemastery
export function apply(ctx: Context, config: Config): void {
  ctx.tools.register(defineTool({ name, description, parameters, output, execute, presentCall }))
}
```

- Registrations are effects (`ctx.effect()`/`ctx.on()`); waterfall listeners MUST call `next()`. No hardcoded tunables — deployment choices are validated `Config` fields from cordis.yml; misconfiguration fails loud.
- `strict: true` + `noImplicitAny`; opaque cross-boundary ids are branded (`Branded<B>` from `dsh-brand`), never bare `string`; closed unions end in `assertNever`; trust TypeScript at typed same-process boundaries (validate only at parser/config, wire, durable, process boundaries).
- Tool design: `presentCall` UI render intent (`generic`/`terminal`/`diff`) decided up front; description instructs the model (see the `todo_write` example).
- Non-trivial changes include an Agent Note in the same PR; tests describe behavior, not correctness; docs/JSDoc accompany code changes.

### Alioth

- **Language**: Chinese default for dialogue, commits, comments, docs; code identifiers English.
- **Naming**: L1 DB physical columns `fk_*`/`qk_*`/`sk_*`/`notice`/`code`; L2 DTO fields carry business semantics (`notice`→`name`); L3 frontend model = DTO 1:1.
- `qk_*` scalar references are `bigint` (Rust `Option<i64>`) — never `DateTime`/`Decimal`/`String`; resolve actual values via `list_refs`/`get_refs`.
- Structured formats (CSS/HTML/JS/JSON/YAML/TOML/SQL) MUST use dedicated parsers (`scripts/parser-utils.mjs` / `scripts/lib/parsers.ts`) — regex-as-parser is forbidden (`docs/specs/NO_REGEX_FOR_PARSING.md`); reuse existing implementations before writing (四查: spec index / scripts / DB facilities / public crates).
- Backend skeleton: `handlers/` + `models/` + `repositories/` + `services/` (see `BACKEND_FRAMEWORK.md`); `decimal` over `double`; frontend state via Jotai v2 (no Zustand/Redux/Recoil); module-local auth middleware forbidden (Gateway owns auth).
- DB is schema truth: `mise run schema-info` for everything; never read DDL files; `isahl` gets views/functions only.

## Important Files

### Harness

- `AGENTS.md` (root standing orders), `docs/AGENTS.md` (doc standards), `docs/architecture.md` (read before touching `packages/`), `packages/README.md` (package map), `docs/testing.md`, `docs/cordis-primer.md`.
- Cookbook: `docs/cookbook/adding-a-tool.md`, `docs/cookbook/adding-a-package.md`, `docs/cookbook/extension-cookbook.md`.
- Reference tool + tests: `packages/todo/tool-todo/src/index.ts`, `packages/todo/tool-todo/tests/tool-todo.spec.ts`.
- Configs: `package.json` (scripts), `pnpm-workspace.yaml`, `tsconfig.host.json`/`tsconfig.client.json` (compiler faces), `tsdown.config.ts`, `vitest*.config.ts`, `.oxlintrc.json`, `lefthook.yml`, `knip.json`.

### Alioth

- `AGENTS.md`, `.agents/critical-rules.md` (hard gates), `.agents/instructions.md` (task → spec/skill loading table).
- Version truth: `Meta/backend/alioth-gen/src/lib.rs`. Ontology: `docs/specs/ALIOTH_ONTOLOGY_SPEC.md`, `ONTOLOGY_REFERENCE.md`; DTO: `DTO_DESIGN_SPEC.md`; backend: `BACKEND_FRAMEWORK.md`; app format: `META_AI_SPEC.md` (+ `BLOCK_SCHEMA.md`, `SERVICE_SPEC.md`, `APP_EXTENSION.md`).
- Concrete artifacts: `Pre-Proc/Alioth/Apps/ai-i-need-a/app.json`, `AppCreator/DESIGN_INTENT.md`, `AppCreator/Pre-Proc/Alioth/` (generated output), `skill-adapters/alioth-app.yaml` (Track/Step/gate shape), `Meta/backend/app-agent/` (pipeline).

## Runtime/Tooling Preferences

- **Harness**: Node `^22.19.0 || >=24.0.0`; pnpm `12.3.4` (workspaces, from `packageManager`); ESM-only; tsx for source launch (`node --import tsx/esm`); build = `tsc -b` + `tsdown`; lint = oxlint; git hooks = lefthook; `DEEPSEEK_API_KEY` required only for e2e/demos/snapshot re-record (keyless replay works without it).
- **Alioth**: mise (Rust backend dev, `mise run dev`), bun (audit/check scripts — new `scripts/check/`/`scripts/ts/` scripts default to bun, Python is legacy), pnpm (frontend workspaces), PostgreSQL with four-layer isolation (`aliothstudio` prod ← `aliothstudio_pre` → `aliothstudio_dev` → `aliothstudio_test`; `DATABASE_URL` must match the intended layer).
- **dsh-alioth plugin**: runs on the harness (Node/pnpm); talking to Alioth means invoking the Rust pipeline (`app-agent`/`alioth-gen`) and/or connecting to the correct PostgreSQL layer.

## Testing & QA

### Harness

- vitest, files named `*.spec.ts` under `tests/` (or colocated), run via `pnpm run test`; **coverage gate is `test:coverage`** — per-file 100% on `packages/*/*/src` (CI enforces).
- Unit pattern (from `tool-todo.spec.ts`): mount the REAL plugin on a real `Context` (+ `SystemPrompt`, `ToolRuntime`, optionally `Loader`), drive through `ctx.tools.execute` with a fake parent `Agent` backed by a real `Session` — only the agent wrapper is a stand-in.
- e2e (`test:e2e`) hits real APIs and self-skips without `DEEPSEEK_API_KEY`; keyless snapshots (`test:snapshot`) replay expected outputs; re-record with `pnpm run test:snapshot:record` (needs key); `pnpm run mock:llm` for local keyless dev.
- Every non-trivial model-visible behavior change adds/updates a keyless snapshot through a real runnable example in the same PR.

### Alioth

- `bash scripts/test-all.sh` (or `mise test`); Rust: `#[tokio::test]` + `connect_test_db()` — `#[sqlx::test]` is forbidden; framework/CRUD crates need unit tests, Service backends need integration tests.
- Frontend is verified against the real backend API (mock data forbidden for integration verification); visual verification via `bun scripts/check/check-visual-verify.ts <prototype>`; full policy in `docs/specs/TEST_INFRASTRUCTURE.md`, `E2E_SPEC.md`.
- Before commit: `npx tsc --noEmit` (TS) + `cargo check` + `clippy` (Rust); conventional commits.
