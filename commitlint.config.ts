/**
 * Commit convention: Conventional Commits (English), enforced by lefthook
 * (commit-msg, local) and CI (over new commits). This repo's history is
 * English conventional commits, matching the host harness convention.
 */
export default { extends: ['@commitlint/config-conventional'] }
